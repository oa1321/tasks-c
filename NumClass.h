#ifndef NumClass
#define NumClass
	
	#define true 1
	#define false 0 
	
	int isArmstrong(int x);
	int isPalindrome(int x);
	int isPrime(int x); //if x is a prime number
	int isStrong(int x); // if x is a strong number
#endif
